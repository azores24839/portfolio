{
  "name": "sihua-portfolio",
  "private": true,
  "scripts": {
    "build:portfolio": "node scripts/build-portfolio.mjs",
    "build": "npm run build:portfolio && node scripts/build-vercel.mjs",
    "upload:r2": "node scripts/upload-r2.mjs",
    "deploy:prepare": "npm run build:portfolio && npm run upload:r2 && npm run build",
    "check": "node --check app.js && node --check portfolio-data.js && node --check scripts/build-vercel.mjs && node --check scripts/upload-r2.mjs && node scripts/build-portfolio.mjs --check"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.840.0",
    "@aws-sdk/lib-storage": "^3.840.0"
  }
}
